/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "vkcfgyyetmhqkafgrrfs.supabase.co"
        ]
    },
    distDir: 'build'
}

module.exports = nextConfig
